<!DOCTYPE html>
<html>
  <head>
    <title>Landing</title>
    <%= stylesheet_link_tag("application") %>
  </head>

  <body>
    <%= link_to(image_tag("journal_icon.png", class: "Journal_Image Top_Height"), root_path) %>
    <h1 class="Top_Title Top_Height"><%= session[:username] %></h1>

    <table class="Menu">
      <tr>
        <td class="Menu_Item No_Underline Lavender"><%= link_to("Home", home_path) %></td>
        <td class="Menu_Item No_Underline Lavender"><%= link_to("My Journal", journal_entries_path) %></td>
        <td class="Menu_Item No_Underline Lavender"><%= link_to("Options", options_path) %></td>
        <td class="Menu_Item No_Underline Lavender"><%= link_to("Log Out", attempt_sign_out_path) %></td>
    </tr>
    </table>

    <%= render partial: 'layouts/notices' %>

    <%= render partial: 'layouts/center_content' %>

  </body>
</html>
